<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yashwant.matchezy">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity
            android:name=".Registration2"
            android:label="@string/title_activity_scrolling"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".Login" />
        <!-- <activity android:name=".Registration" /> -->
        <!-- <activity android:name=".Registration2" /> -->
        <activity android:name=".IntroActivity" />
        <activity android:name=".Registration3" />
        <activity
            android:name=".HomeScreen"
            android:label="@string/title_activity_home_screen" />
        <!-- <activity android:name=".OTP"></activity> -->
        <activity android:name=".ForgotPassword" />
        <activity android:name=".LanguagesPopUp" />
        <activity android:name=".Registration4" />
        <activity android:name=".ProfilePage" />
        <!-- <activity android:name=".Imageupload"></activity> -->
        <!--
            <activity
            android:name=".ScrollingActivity"
            android:label="@string/title_activity_scrolling"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"></activity>
        -->
        <activity android:name=".Registration_Interests"></activity>
    </application>

</manifest>